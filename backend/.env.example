# Database Configuration
# SECURITY: Replace with your actual Neon PostgreSQL connection string
# Get from Neon console: https://console.neon.tech
# Example format: postgresql://user:password@endpoint-pooler.c.us-east-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=postgresql://user:password@your-neon-endpoint.neon.tech/dbname?sslmode=require

# Authentication (Spec 002)
# CRITICAL: BETTER_AUTH_SECRET must be minimum 32 characters and IDENTICAL on frontend and backend
# Generate with: openssl rand -base64 48
# This secret is used for JWT signature verification (stateless authentication)
BETTER_AUTH_SECRET=your_secret_key_minimum_32_characters_long_replace_me

# CORS Configuration (Spec 002)
# SECURITY: Specify allowed origins for frontend requests (comma-separated)
# For development: http://localhost:3000
# For production: https://yourdomain.com
CORS_ORIGINS=http://localhost:3000
