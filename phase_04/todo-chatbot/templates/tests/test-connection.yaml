apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taskie.fullname" . }}-test-connection"
  labels:
    {{- include "taskie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taskie.fullname" . }}-backend:{{ .Values.backend.service.port }}']
  restartPolicy: Never
